import React from "react";
import * as styles from "./styles";

const Header = (props) => {
  return (
    <header css={styles.header}>
      <div css={styles.nav}></div>
    </header>
  );
};

export default Header;
